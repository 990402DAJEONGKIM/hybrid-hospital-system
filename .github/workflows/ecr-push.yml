name: Build, Push & Deploy to ECS

on:
  push:
    branches: [main]
    paths:
      - 'app/combined/**'
      - 'docker/api/**'
      - 'docker/nginx/**'
  workflow_dispatch:

env:
  AWS_REGION: ap-south-2
  ECS_CLUSTER: aws-ecs-cluster-01

jobs:
  # ── 1단계: 이미지 빌드 & ECR 푸시 (2개 병렬) ─────────────────
  build:
    name: Build & Push Images
    runs-on: ubuntu-latest

    permissions:
      id-token: write
      contents: read

    outputs:
      registry: ${{ steps.login-ecr.outputs.registry }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}
          sts-regional-endpoints: regional

      - name: Login to ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build all images (parallel)
        env:
          REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          TAG: ${{ github.sha }}
        run: |
          docker build -f docker/api/Dockerfile \
            -t $REGISTRY/aws-hospital-api:$TAG . &
          PID1=$!
          docker build -f docker/nginx/Dockerfile \
            -t $REGISTRY/aws-hospital-nginx:$TAG . &
          PID2=$!
          wait $PID1 $PID2

      - name: Push all images (parallel)
        env:
          REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          TAG: ${{ github.sha }}
        run: |
          docker push $REGISTRY/aws-hospital-api:$TAG &
          PID1=$!
          docker push $REGISTRY/aws-hospital-nginx:$TAG &
          PID2=$!
          wait $PID1 $PID2

  # ── 2단계: hospital-service 배포 ─────────────────────────────
  deploy:
    name: Deploy hospital-service
    needs: build
    runs-on: ubuntu-latest

    permissions:
      id-token: write
      contents: read

    env:
      REGISTRY: ${{ needs.build.outputs.registry }}
      TAG: ${{ github.sha }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}
          sts-regional-endpoints: regional

      - name: Deploy hospital-service
        run: |
          aws ecs describe-task-definition \
            --task-definition hospital-task \
            --query taskDefinition > hospital-task-def.json

          python3 - <<'EOF'
          import json, os

          with open('hospital-task-def.json') as f:
              td = json.load(f)

          registry = os.environ['REGISTRY']
          tag      = os.environ['TAG']

          for c in td['containerDefinitions']:
              if c['name'] == 'nginx':
                  c['image'] = f"{registry}/aws-hospital-nginx:{tag}"
              elif c['name'] == 'api':
                  c['image'] = f"{registry}/aws-hospital-api:{tag}"

          for key in ['taskDefinitionArn','revision','status','requiresAttributes',
                      'compatibilities','registeredAt','registeredBy',
                      'enableFaultInjection']:
              td.pop(key, None)

          with open('new-hospital-task-def.json', 'w') as f:
              json.dump(td, f)
          EOF

          NEW_ARN=$(aws ecs register-task-definition \
            --cli-input-json file://new-hospital-task-def.json \
            --query 'taskDefinition.taskDefinitionArn' --output text)
          echo "Registered task definition: $NEW_ARN"

          aws ecs update-service \
            --cluster $ECS_CLUSTER \
            --service hospital-service \
            --task-definition $NEW_ARN \
            --output text --query 'service.deployments[0].rolloutState'

          echo "Waiting for hospital-service to stabilize (max 30 min)..."
          for i in $(seq 1 60); do
            RESULT=$(aws ecs describe-services \
              --cluster $ECS_CLUSTER \
              --services hospital-service \
              --query 'services[0].deployments[?status==`PRIMARY`].[rolloutState,desiredCount,runningCount]' \
              --output text)
            STATE=$(echo "$RESULT" | awk '{print $1}')
            DESIRED=$(echo "$RESULT" | awk '{print $2}')
            RUNNING=$(echo "$RESULT" | awk '{print $3}')
            echo "[${i}/60] hospital-service: state=$STATE desired=$DESIRED running=$RUNNING"
            if [ "$STATE" = "COMPLETED" ]; then
              echo "hospital-service deployment succeeded."
              break
            elif [ "$STATE" = "FAILED" ]; then
              echo "hospital-service deployment FAILED."
              exit 1
            fi
            if [ $i -eq 60 ]; then
              echo "Timed out waiting for hospital-service. Check ECS console."
              exit 1
            fi
            sleep 30
          done
