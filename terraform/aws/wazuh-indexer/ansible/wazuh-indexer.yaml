---
- name: Wazuh Indexer 설치
  hosts: wazuh-indexer
  become: yes
  vars_files:
    - vault.yml

  handlers:
    - name: systemd reload
      command: systemctl daemon-reload
      changed_when: false

  tasks:

    # ──────────────────────────────────────────
    # 시스템 기본 설정
    # ──────────────────────────────────────────
    - name: swap 생성
      shell: |
        fallocate -l 4G /swapfile
        chmod 600 /swapfile
        mkswap /swapfile
        swapon /swapfile
        grep -q '/swapfile' /etc/fstab || echo '/swapfile none swap sw 0 0' >> /etc/fstab
      args:
        creates: /swapfile

    - name: vm.swappiness 설정
      sysctl:
        name: vm.swappiness
        value: '10'
        state: present
        reload: yes

    - name: vm.max_map_count 설정
      sysctl:
        name: vm.max_map_count
        value: '262144'
        state: present
        reload: yes

    # ──────────────────────────────────────────
    # Wazuh 버전 고정
    # ──────────────────────────────────────────
    - name: Wazuh 버전 고정
      copy:
        dest: /etc/apt/preferences.d/wazuh
        content: |
          Package: wazuh-indexer
          Pin: version 4.14.5*
          Pin-Priority: 1001

    - name: 시스템 업데이트
      apt:
        update_cache: yes
        upgrade: safe
      timeout: 300

    - name: 필수 패키지 설치
      apt:
        name:
          - curl
          - apt-transport-https
          - gnupg
          - python3
          - awscli
        state: present

    # ──────────────────────────────────────────
    # 설치 여부 확인 (indexer 기준)
    # ──────────────────────────────────────────
    - name: Wazuh Indexer 설치 여부 확인
      stat:
        path: /etc/wazuh-indexer/opensearch.yml
      register: wazuh_indexer_installed

    # ──────────────────────────────────────────
    # 설치 블록 (최초 1회)
    # ──────────────────────────────────────────
    - name: Wazuh Indexer 신규 설치
      when: not wazuh_indexer_installed.stat.exists
      block:
        - name: Wazuh 잔재 정리
          shell: |
            systemctl stop wazuh-indexer 2>/dev/null || true
            apt purge wazuh-indexer -y || true
            apt autoremove -y || true
            dpkg --configure -a || true
            apt-get install -f -y || true
            rm -rf /etc/wazuh-indexer || true
            rm -rf /usr/share/wazuh-indexer || true
            rm -rf /var/lib/wazuh-indexer || true
            systemctl daemon-reload || true
            systemctl reset-failed || true

        - name: wazuh-install.sh 다운로드
          get_url:
            url: 'https://packages.wazuh.com/4.14/wazuh-install.sh'
            dest: '/tmp/wazuh-install.sh'
            mode: '0755'

        - name: 이전 설치 파일 정리
          file:
            path: "{{ item }}"
            state: absent
          loop:
            - /tmp/wazuh-install-files
            - /tmp/wazuh-install-files.tar

        - name: 다중 노드 config.yml 작성
          copy:
            dest: /tmp/config.yml
            content: |
              nodes:
                indexer:
                  - name: node-1
                    ip: "{{ ansible_default_ipv4.address }}"
                server:
                  - name: wazuh-1
                    ip: "{{ wazuh_manager1_ip }}"
                    node_type: master
                  - name: wazuh-2
                    ip: "{{ wazuh_manager2_ip }}"
                    node_type: worker
                dashboard:
                  - name: dashboard-1
                    ip: "{{ wazuh_manager1_ip }}"
                  - name: dashboard-2
                    ip: "{{ wazuh_manager2_ip }}"

        - name: 인증서 생성
          shell: bash /tmp/wazuh-install.sh --generate-config-files
          args:
            chdir: /tmp/
          async: 600
          poll: 30

        - name: wazuh-install-files.tar S3 업로드 (Manager EC2들이 사용)
          shell: >
            aws s3 cp /tmp/wazuh-install-files.tar
            s3://wazuh-ansible-ssm/wazuh-install-files.tar
            --region {{ ansible_env.AWS_DEFAULT_REGION | default('ap-south-2') }}
          environment:
            AWS_DEFAULT_REGION: "ap-south-2"

        - name: Wazuh Indexer 설치
          shell: bash /tmp/wazuh-install.sh --wazuh-indexer node-1 --overwrite
          args:
            chdir: /tmp/
          async: 1800
          poll: 30

        - name: Indexer API 준비 대기 (start-cluster 실행 전)
          wait_for:
            port: 9200
            host: "{{ ansible_default_ipv4.address }}"
            delay: 15
            timeout: 300
            msg: "Indexer가 start-cluster 전에 응답하지 않습니다."

        - name: Wazuh Cluster 시작
          shell: bash /tmp/wazuh-install.sh --start-cluster
          args:
            chdir: /tmp/
          async: 600
          poll: 30

      rescue:
        - name: 설치 실패 로그 출력
          shell: tail -50 /var/log/wazuh-install.log
          register: wazuh_log
          failed_when: false

        - name: 에러 로그 출력
          debug:
            var: wazuh_log.stdout_lines

        - name: 설치 실패 처리
          fail:
            msg: "Wazuh Indexer 설치 실패. 위 로그를 확인하세요."

    # ──────────────────────────────────────────
    # 설정 블록 (항상 실행)
    # ──────────────────────────────────────────
    - name: JVM 힙 메모리 설정
      replace:
        path: /etc/wazuh-indexer/jvm.options
        regexp: "{{ item.regexp }}"
        replace: "{{ item.replace }}"
      loop:
        - { regexp: '^-Xms.*', replace: '-Xms4g' }
        - { regexp: '^-Xmx.*', replace: '-Xmx4g' }

    - name: Indexer 메모리 lock 설정
      lineinfile:
        path: /etc/wazuh-indexer/opensearch.yml
        regexp: '^bootstrap.memory_lock:'
        line: 'bootstrap.memory_lock: true'
        state: present

    - name: systemd override 디렉토리 생성
      file:
        path: /etc/systemd/system/wazuh-indexer.service.d
        state: directory
        mode: '0755'

    - name: Indexer systemd 재시작 설정
      copy:
        dest: /etc/systemd/system/wazuh-indexer.service.d/restart.conf
        content: |
          [Unit]
          StartLimitIntervalSec=300
          StartLimitBurst=5

          [Service]
          Restart=always
          RestartSec=30
          LimitMEMLOCK=infinity
      notify: systemd reload

    - meta: flush_handlers

    # ──────────────────────────────────────────
    # 서비스 재시작
    # ──────────────────────────────────────────
    - name: Wazuh Indexer 재시작
      systemd:
        name: wazuh-indexer
        state: restarted
        enabled: yes

    - name: Wazuh Indexer API 응답 대기
      wait_for:
        port: 9200
        host: "{{ ansible_default_ipv4.address }}"
        delay: 15
        timeout: 300
        msg: "Indexer가 제한 시간 내에 시작되지 않았습니다."

    - name: Wazuh admin 비밀번호 고정
      shell: >
        timeout 60
        /usr/share/wazuh-indexer/plugins/opensearch-security/tools/wazuh-passwords-tool.sh
        -u admin -p '{{ wazuh_admin_password }}'
      no_log: true
      changed_when: false
      register: pwd_change
      until: pwd_change.rc == 0
      retries: 5
      delay: 10

    - name: kibanaserver 비밀번호 고정
      shell: >
        timeout 60
        /usr/share/wazuh-indexer/plugins/opensearch-security/tools/wazuh-passwords-tool.sh
        -u kibanaserver -p '{{ kibanaserver_password }}'
      no_log: true
      changed_when: false
      register: kibana_pwd_change
      until: kibana_pwd_change.rc == 0
      retries: 5
      delay: 10

    # ──────────────────────────────────────────
    # 결과 확인
    # ──────────────────────────────────────────
    - name: 서비스 상태 확인
      shell: |
        echo "=== Indexer ===" && systemctl is-active wazuh-indexer
      register: service_status

    - name: 서비스 상태 출력
      debug:
        var: service_status.stdout_lines

    - name: 메모리 상태 확인
      command: free -h
      register: memory_status

    - name: 메모리 상태 출력
      debug:
        var: memory_status.stdout_lines
