<agent_config>
  <client_buffer>
    <disabled>no</disabled>
    <queue_size>50000</queue_size>
    <events_per_second>500</events_per_second>
  </client_buffer>

  <wodle name="syscollector">
    <disabled>no</disabled>
    <interval>1h</interval>
    <scan_on_start>yes</scan_on_start>
    <packages>yes</packages>
    <os>yes</os>
    <processes>yes</processes>
  </wodle>

  <syscheck>
    <directories realtime="yes" check_all="yes" report_changes="yes">/etc</directories>
    <directories realtime="yes" check_all="yes">/root/.ssh</directories>
    <directories realtime="yes" check_all="yes">/opt/audit-collector</directories>
    <directories check_all="yes">/usr/bin,/usr/sbin</directories>
    <ignore>/etc/mtab</ignore>
    <ignore>/etc/resolv.conf</ignore>
    <ignore>/etc/adjtime</ignore>
  </syscheck>

  <!-- SSH 로그인/실패 -->
  <localfile>
    <log_format>syslog</log_format>
    <location>/var/log/auth.log</location>
  </localfile>

  <!-- 시스템 로그 -->
  <localfile>
    <log_format>syslog</log_format>
    <location>/var/log/syslog</location>
  </localfile>

  <!-- HAProxy 로그 -->
  <localfile>
    <log_format>syslog</log_format>
    <location>/var/log/haproxy.log</location>
  </localfile>

  <!-- audit collector 로그 -->
  <localfile>
    <log_format>json</log_format>
    <location>/var/log/cloudsql-audit.log</location>
  </localfile>
</agent_config>