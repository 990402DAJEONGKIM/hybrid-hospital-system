<agent_config>
  <client_buffer>
    <disabled>no</disabled>
    <queue_size>50000</queue_size>
    <events_per_second>500</events_per_second>
  </client_buffer>

  <wodle name="syscollector">
    <disabled>no</disabled>
    <interval>1h</interval>
    <scan_on_start>yes</scan_on_start>
    <packages>yes</packages>
    <os>yes</os>
    <processes>yes</processes>
  </wodle>

  <!-- FIM - ISMS-P 2.9.1 - 추가 260612 김강환 -->
  <syscheck>
    <directories realtime="yes" check_all="yes" report_changes="yes">/etc</directories>
    <ignore>/etc/mtab</ignore>
    <ignore>/etc/resolv.conf</ignore>
    <ignore>/etc/adjtime</ignore>
    <ignore type="sregex">\.swp$</ignore>
  </syscheck>

  <!-- sudo 기록 - ISMS-P 2.5.1 -->
  <localfile>
    <log_format>syslog</log_format>
    <location>/var/log/auth.log</location>
  </localfile>

  <!-- Nginx 접근 기록 (Keycloak) - ISMS-P 2.9.1 - 추가 260612 김강환 -->
  <localfile>
    <log_format>apache</log_format>
    <location>/var/log/nginx/access.log</location>
  </localfile>

  <!-- Grafana 서비스 로그 - 수정 260614 김강환 -->
  <!-- journald → 파일 방식으로 변경 (journald 미지원 확인) -->
  <localfile>
    <log_format>syslog</log_format>
    <location>/var/log/grafana/grafana.log</location>
  </localfile>

  <!-- Prometheus 서비스 로그 - 수정 260614 김강환 -->
  <!-- journald → 파일 방식으로 변경 (journald 미지원 확인) -->
  <localfile>
    <log_format>syslog</log_format>
    <location>/var/log/prometheus/prometheus.log</location>
  </localfile>

  <!-- Grafana 서비스 상태 감시 - 수정 260614 김강환 -->
  <localfile>
    <location>journald</location>
    <log_format>journald</log_format>
    <filter field="_SYSTEMD_UNIT">grafana-server.service</filter>
  </localfile>

  <!-- Prometheus 서비스 상태 감시 - 수정 260614 김강환 -->
  <localfile>
    <location>journald</location>
    <log_format>journald</log_format>
    <filter field="_SYSTEMD_UNIT">prometheus.service</filter>
  </localfile>

  <!-- kernel/UFW 로그 - 수정 260614 김강환 -->
  <localfile>
    <location>journald</location>
    <log_format>journald</log_format>
    <filter field="_TRANSPORT">kernel</filter>
  </localfile>

</agent_config>