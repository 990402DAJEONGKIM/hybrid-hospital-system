<group name="ecs,fastapi,">

  <!-- ECS FastAPI 감사 로그 베이스 룰 - 수정 260616 김강환 -->
  <rule id="100700" level="0">
    <if_sid>86600</if_sid>
    <field name="event_type">^fastapi_audit$</field>
    <field name="source_type">^docker_logs$</field>
    <description>ECS FastAPI 감사 로그 베이스</description>
    <group>ecs,fastapi,audit_archive,</group>
  </rule>

  <!-- 정상 응답 억제 -->
  <rule id="100703" level="0">
    <if_sid>100700</if_sid>
    <field name="result_code">^2</field>
    <description>ECS FastAPI 정상 응답 — 무시</description>
    <group>noise,</group>
  </rule>

  <!-- 인증 실패 카운터 (대시보드 안 뜸, 브루트포스 카운터 역할) -->
  <rule id="100701" level="0">
    <if_sid>100700</if_sid>
    <field name="result_code">^401$|^403$</field>
    <description>ECS FastAPI 인증 실패 카운터</description>
    <group>ecs,fastapi,authentication_failed,</group>
    <mitre><id>T1110</id></mitre>
  </rule>

  <!-- 브루트포스 — Slack -->
  <rule id="100702" level="12" frequency="8" timeframe="120" ignore="60">
    <if_matched_sid>100701</if_matched_sid>
    <same_field>source_ip</same_field>
    <description>ECS FastAPI 브루트포스 의심 — 공격 IP: $(source_ip)</description>
    <group>ecs,fastapi,brute_force,</group>
    <mitre><id>T1110.001</id></mitre>
  </rule>

  <!-- UNKNOWN 경로 접근 — 스캐너/해커 탐색 의심 -->
  <rule id="100705" level="8">
    <if_sid>100700</if_sid>
    <field name="action_type">^UNKNOWN$</field>
    <description>ECS FastAPI 미정의 경로 접근 — IP: $(source_ip) 경로: $(path)</description>
    <group>ecs,fastapi,recon,</group>
    <mitre><id>T1595</id></mitre>
  </rule>


</group>