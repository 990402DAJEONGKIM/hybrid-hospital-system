---
- name: Wazuh Manager + Dashboard 설치
  hosts: wazuh
  become: yes
  vars_files:
    - vault.yml

  handlers:
    - name: systemd reload
      command: systemctl daemon-reload
      changed_when: false

  tasks:

    # ──────────────────────────────────────────
    # 시스템 기본 설정
    # ──────────────────────────────────────────
    - name: swap 생성
      shell: |
        fallocate -l 4G /swapfile
        chmod 600 /swapfile
        mkswap /swapfile
        swapon /swapfile
        grep -q '/swapfile' /etc/fstab || echo '/swapfile none swap sw 0 0' >> /etc/fstab
      args:
        creates: /swapfile

    - name: vm.swappiness 설정
      sysctl:
        name: vm.swappiness
        value: '10'
        state: present
        reload: yes

    - name: vm.max_map_count 설정
      sysctl:
        name: vm.max_map_count
        value: '262144'
        state: present
        reload: yes

    # ──────────────────────────────────────────
    # Wazuh 버전 고정 (indexer 제외)
    # ──────────────────────────────────────────
    - name: Wazuh 버전 고정
      copy:
        dest: /etc/apt/preferences.d/wazuh
        content: |
          Package: wazuh-manager wazuh-dashboard
          Pin: version 4.14.5*
          Pin-Priority: 1001

    - name: 시스템 업데이트
      apt:
        update_cache: yes
        upgrade: safe
      timeout: 300

    - name: 필수 패키지 설치
      apt:
        name:
          - curl
          - apt-transport-https
          - gnupg
          - python3
          - libxml2-utils
          - awscli
        state: present

    # ──────────────────────────────────────────
    # 설치 여부 확인 (manager 기준)
    # ──────────────────────────────────────────
    - name: Wazuh 설치 여부 확인
      stat:
        path: /var/ossec/etc/ossec.conf
      register: wazuh_installed

    # ──────────────────────────────────────────
    # 설치 블록 (최초 1회)
    # ──────────────────────────────────────────
    - name: Wazuh 신규 설치
      when: not wazuh_installed.stat.exists
      block:
        - name: Wazuh 잔재 정리
          shell: |
            systemctl stop wazuh-manager 2>/dev/null || true
            systemctl stop wazuh-dashboard 2>/dev/null || true
            apt purge wazuh-manager wazuh-dashboard -y || true
            apt autoremove -y || true
            dpkg --configure -a || true
            apt-get install -f -y || true
            rm -rf /etc/wazuh-dashboard || true
            rm -rf /var/ossec || true
            rm -rf /usr/share/wazuh-dashboard || true
            systemctl daemon-reload || true
            systemctl reset-failed || true

        - name: wazuh-install.sh 다운로드
          get_url:
            url: 'https://packages.wazuh.com/4.14/wazuh-install.sh'
            dest: '/tmp/wazuh-install.sh'
            mode: '0755'

        - name: 이전 설치 파일 정리
          file:
            path: "{{ item }}"
            state: absent
          loop:
            - /tmp/wazuh-install-files
            - /tmp/wazuh-install-files.tar

        - name: wazuh-install-files.tar S3 다운로드 (Indexer가 생성한 인증서)
          shell: >
            aws s3 cp s3://wazuh-ansible-ssm/wazuh-install-files.tar /tmp/wazuh-install-files.tar
            --region ap-south-2
          environment:
            AWS_DEFAULT_REGION: "ap-south-2"

        - name: Wazuh Manager 설치 (worker)
          shell: bash /tmp/wazuh-install.sh --wazuh-server wazuh-2 --overwrite
          args:
            chdir: /tmp/
          async: 1800
          poll: 30

        - name: Wazuh Dashboard 설치
          shell: bash /tmp/wazuh-install.sh --wazuh-dashboard dashboard-2 --overwrite
          args:
            chdir: /tmp/
          async: 1800
          poll: 30

      rescue:
        - name: 설치 실패 로그 출력
          shell: tail -50 /var/log/wazuh-install.log
          register: wazuh_log
          failed_when: false

        - name: 에러 로그 출력
          debug:
            var: wazuh_log.stdout_lines

        - name: 설치 실패 처리
          fail:
            msg: "Wazuh 설치 실패. 위 로그를 확인하세요."

    # ──────────────────────────────────────────
    # 설정 블록 (항상 실행)
    # ──────────────────────────────────────────
    - name: 로컬 Indexer 서비스 중지 (외부 Indexer로 전환)
      systemd:
        name: wazuh-indexer
        state: stopped
        enabled: no
      ignore_errors: yes

    - name: 최신 인증서 S3에서 다운로드
      shell: >
        aws s3 cp s3://wazuh-ansible-ssm/wazuh-install-files.tar
        /tmp/wazuh-install-files.tar --region ap-south-2
      environment:
        AWS_DEFAULT_REGION: "ap-south-2"

    - name: 인증서 압축 해제
      shell: tar -xf /tmp/wazuh-install-files.tar -C /tmp/

    - name: Filebeat 인증서 갱신
      shell: |
        cp /tmp/wazuh-install-files/{{ wazuh_cert_name }}.pem /etc/filebeat/certs/
        cp /tmp/wazuh-install-files/{{ wazuh_cert_name }}-key.pem /etc/filebeat/certs/
        cp /tmp/wazuh-install-files/root-ca.pem /etc/filebeat/certs/
        cp /tmp/wazuh-install-files/root-ca.pem /etc/wazuh-dashboard/certs/
        
    - name: ossec.conf 템플릿 배포
      template:
        src: ossec.conf.j2
        dest: /var/ossec/etc/ossec.conf
        owner: root
        group: wazuh
        mode: '0640'

    - name: 커스텀 룰 배포
      copy:
        src: local_rules.xml
        dest: /var/ossec/etc/rules/local_rules.xml
        owner: root
        group: wazuh
        mode: '0640'

    - name: Agent 중앙 설정 배포
      copy:
        src: agent.conf
        dest: /var/ossec/etc/shared/default/agent.conf
        owner: wazuh
        group: wazuh
        mode: '0640'

    - name: ossec.conf XML 검증
      shell: |
        python3 -c "
        import xml.etree.ElementTree as ET
        ET.parse('/var/ossec/etc/ossec.conf')
        print('XML OK')
        "
      register: xml_check
      failed_when: "'XML OK' not in xml_check.stdout"

    - name: Wazuh 설정 파서 검증
      command: /var/ossec/bin/wazuh-analysisd -t
      register: analysisd_check
      failed_when: analysisd_check.rc != 0

    - name: analysisd 검증 결과 출력
      debug:
        var: analysisd_check.stdout_lines
      when: analysisd_check.stdout_lines is defined

    - name: Dashboard server.host 설정
      lineinfile:
        path: /etc/wazuh-dashboard/opensearch_dashboards.yml
        regexp: '^server.host:'
        line: 'server.host: 0.0.0.0'

    - name: Filebeat 설정 배포
      template:
        src: filebeat.yml.j2
        dest: /etc/filebeat/filebeat.yml
        owner: root
        group: root
        mode: '0600'

    - name: Dashboard opensearch.hosts
      replace:
        path: /etc/wazuh-dashboard/opensearch_dashboards.yml
        regexp: 'opensearch.hosts:.*'
        replace: 'opensearch.hosts: ["https://{{ wazuh_indexer_ip }}:9200"]'

    - name: systemd override 디렉토리 생성
      file:
        path: "/etc/systemd/system/{{ item }}.service.d"
        state: directory
        mode: '0755'
      loop:
        - wazuh-manager
        - wazuh-dashboard

    - name: Manager systemd 재시작 설정
      copy:
        dest: /etc/systemd/system/wazuh-manager.service.d/restart.conf
        content: |
          [Unit]
          StartLimitIntervalSec=300
          StartLimitBurst=5

          [Service]
          Restart=always
          RestartSec=10
      notify: systemd reload

    - name: Dashboard systemd 재시작 설정
      copy:
        dest: /etc/systemd/system/wazuh-dashboard.service.d/restart.conf
        content: |
          [Unit]
          StartLimitIntervalSec=300
          StartLimitBurst=5

          [Service]
          Restart=always
          RestartSec=10
      notify: systemd reload

    - meta: flush_handlers

    - name: Dashboard keystore kibanaserver 비밀번호 업데이트
      shell: |
        echo '{{ kibanaserver_password }}' | \
        sudo -u wazuh-dashboard \
        NODE_OPTIONS="--max-old-space-size=2048" \
        /usr/share/wazuh-dashboard/bin/opensearch-dashboards-keystore \
        add opensearch.password --force --stdin
      no_log: true

    # ──────────────────────────────────────────
    # 서비스 재시작
    # ──────────────────────────────────────────
    - name: Wazuh 서비스 재시작
      systemd:
        name: "{{ item }}"
        state: restarted
        enabled: yes
      loop:
        - filebeat
        - wazuh-manager
        - wazuh-dashboard

    # ──────────────────────────────────────────
    # 결과 확인
    # ──────────────────────────────────────────
    - name: 서비스 상태 확인
      shell: |
        echo "=== Manager ===" && systemctl is-active wazuh-manager
        echo "=== Dashboard ===" && systemctl is-active wazuh-dashboard
      register: service_status

    - name: 서비스 상태 출력
      debug:
        var: service_status.stdout_lines

    - name: 메모리 상태 확인
      command: free -h
      register: memory_status

    - name: 메모리 상태 출력
      debug:
        var: memory_status.stdout_lines
